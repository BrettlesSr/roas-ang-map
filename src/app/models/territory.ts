export class Territory {
    key: string;

    name: string;
    description: string;
    thumbnail: string;
    threadlink: string;

    owner: string; // must be a polity name
    star: string; // must be a star name
}
